# app keys..
"chromium"
  Mod4 + c
"firefox"
  Mod4 + f
"~/bin/background/screenshot"
  Mod4 + a
"urxvt"
  Mod4 + t
"sleep .1; xdotool click 1"
  Mod4 + Return
"~/bin/background/launcher"
  Mod4 + r
"~/bin/xclick.py ~/bin/imgs/zeal-tray.png"
  Mod4 + z
"simpleswitcher -now"
  Mod4 + Tab
"chromium --profile-directory=Default --app-id=hmjkmjkepdijhoojdojkdfohbdgmmhki"
  Mod4 + g
"~/bin/email-client"
  Mod4 + e
"slock"
  Control+Alt+Shift+l
"xkill"
  Control+Alt+Shift+k
"cd /tmp && scrot && sleep .5 && notify-send 'Screenshot saved to /tmp'"
  Control+Alt+Shift+a

# audio keys..
"banshee --toggle-playing"
  XF86AudioPlay
"banshee --next"
  XF86AudioNext
"banshee --previous"
  XF86AudioPrev
"banshee --stop"
  XF86AudioStop

# Logitech Mouse..
# TODO use awesome-client for switch, and volume
"xdotool key --clearmodifiers alt+Tab"
  XF86Mail	# the forward button, depends on xcape rules
"notify-send comb"
  Mod4 + XF86Mail	# the backward button
# TODO hold htop to the middle, and release
"notify-send launch1"
  XF86Launch1
"notify-send launch2"
  XF86Launch2

# Right click (conflict with awesome move window, use on macbook)
#"sleep .04 && xdotool click 3"
  #Release + Mod4 + b:1

"sleep .04 && xdotool key --clearmodifiers Delete"
  shift + BackSpace

