# app keys..

"chromium --force-device-scale-factor=1.2 --no-proxy-server"
  Mod4 + c
"~/bin/background/screenshot"
  Mod4 + a
"alacritty"
  Mod4 + t
"sleep .1; xdotool click 1"
  Mod4 + Return

"rofi -show run"
  Mod4 + r
"rofi -show window"
  Mod4 + Tab
"rofi -sort -matching fuzzy -show file -modi "file:~/bin/background/rofi-file-browser $HOME/Documents/books""
  Mod4 + b

"zeal"
  Mod4 + z
"chromium --profile-directory=Default --app-id=hmjkmjkepdijhoojdojkdfohbdgmmhki"
  Mod4 + g

#"~/bin/background/email-client"
  #Mod4 + e

#"slock"
"gnome-screensaver-command --lock"
  Control+Alt+Shift+l
"xkill"
  Control+Alt+Shift+k
"cd /tmp && scrot -z && sleep .5 && notify-send 'Screenshot saved to /tmp'"
  Control+Alt+Shift+a

# audio keys..
"banshee --toggle-playing"
  XF86AudioPlay
"banshee --next"
  XF86AudioNext
"banshee --previous"
  XF86AudioPrev
"banshee --stop"
  XF86AudioStop

# Mouse..
# TODO use awesome-client for switch, and volume


# backward
"chromium https://our.intern.facebook.com/intern/diffs/"
  m:0x0 + b:8
# forward
"chromium https://github.com/facebookresearch/detectron2/issues"
  m:0x0 + b:9
# forward
"notify-send button10"
  m:0x0 + b:10

# "notify-send mail"
#   XF86Mail	# the forward button, depends on xcape rules
# "notify-send mod4+mail"
#   Mod4 + XF86Mail	# the backward button
"notify-send launch1"
  XF86Launch1
"notify-send launch2"
  XF86Launch2

# Right click (conflict with awesome move window, use on macbook)
#"sleep .04 && xdotool click 3"
  #Release + Mod4 + b:1

"sleep .04 && xdotool key --clearmodifiers Delete"
  shift + BackSpace
